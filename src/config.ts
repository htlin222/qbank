// Application configuration
const config = {
  // Application title - uses the VITE_APP_TITLE from .env file
  appTitle: import.meta.env.VITE_APP_TITLE,
  
  // Add other configuration variables as needed
};

export default config;
